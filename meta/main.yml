#SPDX-License-Identifier: MIT-0
galaxy_info:
  author: David Igou
  description: Downloads openshift-install and renders agent install objects
  role_name: openshift_agent_install
  namespace: david-igou
  license: MIT
  min_ansible_version: "2.15"
  galaxy_tags:
    - openshift
    - kubernetes
    - agent
    - pxe
    - installer

dependencies: []
  # List your role dependencies here, one per line. Be sure to remove the '[]' above,
  # if you add dependencies to this list.
